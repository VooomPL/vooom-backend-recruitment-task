import { TopUpCompanyCommand } from './top-up-company.command';

describe('TopUpCompanyCommand', () => {
  it('should be defined', () => {
    expect(new TopUpCompanyCommand()).toBeDefined();
  });
});
